import React from 'react';

import classes from './styles.module.css';

const Header = (props) => {
    
    return (
        <header className={classes.header}>
            <h1>Procrastinator Motivator</h1>
        </header>
    );
};

export default Header;